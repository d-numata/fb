<h2>フォローされているユーザー一覧</h2>
<% @user.followers.each do |user| %>
 <p><%= user.name %>
 <%= profile_img(user)%>
 <%= user.email %></p>
<% end %>

<h2>フォローしているユーザー一覧</h2>
<% @user.followed_users.each do |user| %>
 <p> <%= user.name %>
 <%= profile_img(user)%>
 <%= user.email %></p>
<% end %>